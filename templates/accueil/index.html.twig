{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    {# {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    {% endif %} #}

    <h1 class="text-center">Welcome to Teachar</h1>
    {% if app.user == false%}
        <h2 class="text-center">Please log in or register to access the rest of the site</h2>
        <div class="row d-flex justify-content-center">
            <a href="{{path('app_login')}}"<button type="button" class="btn btn-outline-info w-25">Login</button></a>
            <a href="{{path('app_register')}}"<button type="button" class="btn btn-outline-info w-25">Register</button></a>
            </div>
    {% endif %}

    {% if is_granted("ROLE_USER")%}
        {% if user.getUserInfo.getUsername == "username" %}
            <h4 class="text-center">Please feel free to change your username and avatar by going to your profile in the dropdown menu located in the top-right of your screen</h2>
        {% endif %}
    {% endif %}

    {% if is_granted("ROLE_TEACHER")%}
        <h1>BIG TEST BIG WIN</h1>
    {% endif %}

    {% if is_granted("ROLE_ADMIN") %}
        <a href="{{path('app_adminPage')}}"<button type="button" class="btn btn-outline-warning">Admin Page</button></a>
    {% endif %}
    
{% endblock %}